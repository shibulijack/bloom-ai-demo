# Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# -----------------------------
# Next.js / Node
# -----------------------------
NODE_ENV=development

# -----------------------------
# NextAuth (v5)
# NEXTAUTH_URL is required for callbacks (production: https://your-domain.vercel.app)
# NEXTAUTH_SECRET should be a long random string (generate with: openssl rand -base64 32)
# AUTH_SECRET is the new unified secret name also read by NextAuth v5
# -----------------------------
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-here
AUTH_SECRET=your-secret-here

# -----------------------------
# Google OAuth
# Create credentials at: https://console.cloud.google.com/apis/credentials
# For production, add your Vercel domain to authorized redirect URIs:
# https://your-domain.vercel.app/api/auth/callback/google
# -----------------------------
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# -----------------------------
# Database
# For production on Vercel, use PostgreSQL (SQLite doesn't work on Vercel)
# You can use Vercel Postgres, Supabase, PlanetScale, or any PostgreSQL provider
# -----------------------------
# Development (SQLite)
DATABASE_URL=file:./dev.db

# Production (PostgreSQL) - uncomment and use for production
# DATABASE_URL=postgresql://username:password@host:port/database?schema=public

# -----------------------------
# Optional future variables
# -----------------------------
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# SENTRY_DSN=
# RESEND_API_KEY=